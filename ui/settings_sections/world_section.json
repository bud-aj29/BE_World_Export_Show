{
  "namespace": "world_section",

  "export_world_button@settings_common.action_button": {
    "ignored": 0 //change boolean to static false. default="(($pocket_edition or $export_disabled or not $file_picking_supported) and not $education_edition)"
  }
}
